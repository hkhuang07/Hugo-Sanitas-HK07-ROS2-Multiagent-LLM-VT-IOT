# ============================================================================
# HK-07 Agent Engine — Python Dependencies
# Pinned for production stability (2026-06)
# ============================================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic>=2.12.5,<3.0.0

# --- Async + HTTP ---
httpx>=0.28.1,<1.0.0
anyio>=4.8.0,<5.0.0
websockets>=12.0

# --- MQTT ---
paho-mqtt==2.1.0

# --- Vector Memory ---
lancedb==0.33.0
pyarrow==16.1.0
numpy==1.26.4

# --- LLM Providers ---
# Groq SDK (thin REST wrapper)
groq==1.5.0

# Google Generative AI — NEW SDK (google-generativeai deprecated June 2026)
# LiteLLM >= 1.89.3 internally uses google-genai for Gemini routing
google-genai==2.10.0

# Legacy compat shim — keep installed but do not import directly in production code
# google-generativeai>=0.8.3

# LiteLLM — unified multi-provider router
# v1.89.3+ patches AQ. key support, gemini/ provider native routing, and
# resolves the protobuf MessageFactory.GetPrototype() deprecation path
litellm==1.90.2

# --- Protobuf: CRITICAL PIN ---
# mediapipe>=0.10.18 requires protobuf>=4.25.3,<5.0.0
# Anything >=5.0.0 removes MessageFactory.GetPrototype() causing AttributeError
protobuf>=4.25.3,<5.0.0

# --- Computer Vision ---
# OpenCV (headless, no GUI deps)
opencv-python-headless==4.10.0.82

# MediaPipe: 0.10.18+ fixes GetPrototype() deprecation on Windows + Python 3.10/3.11
# Provides: Pose (33 keypoints), FaceMesh (468 landmarks), Hands
mediapipe>=0.10.18

# --- Env / Config ---
python-dotenv>=1.0.0

# --- Local SLM (optional — install separately if needed) ---
# Heavy build: requires MSVC on Windows. Skipped in base requirements.
# Install manually: pip install llama-cpp-python>=0.2.75
# llama-cpp-python>=0.2.75

# --- Redis (for Blackboard — optional, falls back to in-memory) ---
# redis>=5.0.0  (auto-detected by BlackboardService at startup)
redis>=5.0.0

# --- Web Crawling for Medical Knowledge RAG ---
beautifulsoup4>=4.12.0
aiohttp>=3.9.0
